<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/style.css">
</head>

<body class="form-page">
    <%- include('partials/header') %>
        <main class="container mx-auto my-5 pt-lg-5">
            <div class="form-card">
                <h3 class="text-center mb-4 fw-semibold" style="color:#6b67fb;">Edit Book Details</h3>

                <form action="/edit-book/<%= editBook._id %>" method="POST">

                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label class="form-label text-black">Book Name :</label>
                            <input type="text" name="bookName" class="form-control" value="<%= editBook.bookName %>"
                                required />
                        </div>

                        <div class="col-md-6">
                            <label class="form-label text-black">Author :</label>
                            <input type="text" name="author" class="form-control" value="<%= editBook.author %>"
                                required />
                        </div>
                    </div>

                    <div class="row g-3 mb-3">
                        <div class="col-md-4">
                            <label class="form-label text-black">Price :</label>
                            <input type="number" name="price" class="form-control" value="<%= editBook.price %>"
                                required />
                        </div>

                        <div class="col-md-4">
                            <label class="form-label text-black">Publisher Date :</label>
                            <input type="date" name="publisherDate" class="form-control"
                                value="<%= editBook.publisherDate %>" required />
                        </div>

                        <div class="col-md-4">
                            <label class="form-label text-black">Genre :</label>
                            <select name="genre" class="form-select" required>
                                <option value="">Select Genre</option>
                                <option <%=editBook.genre=="Classic" ? "selected" : "" %>>Classic</option>
                                <option <%=editBook.genre=="Science" ? "selected" : "" %>>Science</option>
                                <option <%=editBook.genre=="Kids" ? "selected" : "" %>>Kids</option>
                                <option <%=editBook.genre=="Comics" ? "selected" : "" %>>Comics</option>
                                <option <%=editBook.genre=="Romance" ? "selected" : "" %>>Romance</option>
                                <option <%=editBook.genre=="Thriller" ? "selected" : "" %>>Thriller</option>
                                <option <%=editBook.genre=="Self-help" ? "selected" : "" %>>Self-help</option>
                            </select>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-black">Format :</label>
                        <div class="d-flex flex-wrap gap-3">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="format" value="Paperback"
                                    <%=editBook.format=="Paperback" ? "checked" : "" %> />
                                <label class="form-check-label">Paperback</label>
                            </div>

                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="format" value="Hardcover"
                                    <%=editBook.format=="Hardcover" ? "checked" : "" %> />
                                <label class="form-check-label">Hardcover</label>
                            </div>

                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="format" value="Ebook"
                                    <%=editBook.format=="Ebook" ? "checked" : "" %> />
                                <label class="form-check-label">Ebook</label>
                            </div>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label text-black">Features :</label>
                        <div class="d-flex flex-wrap gap-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" name="features" value="Bestseller"
                                    <%=editBook.features.includes("Bestseller") ? "checked" : "" %> />
                                <label class="form-check-label">Bestseller</label>
                            </div>

                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" name="features" value="New Arrival"
                                    <%=editBook.features.includes("New Arrival") ? "checked" : "" %> />
                                <label class="form-check-label">New Arrival</label>
                            </div>
                        </div>
                    </div>

                    <button class="btn-custom">
                        <span>
                            Update
                        </span>
                    </button>
                </form>
            </div>
        </main>

</body>

<script src="/assets/js/Bootstrap.min.js"></script>
<script src="/assets/js/script.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

</html>